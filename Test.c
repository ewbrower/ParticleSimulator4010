// 

int TestingSuite(struct Particle* particles, bool randSpeed, bool randDirect)
{
    if (randSpeed > 1 || randDirect > 1)
    {
        return -1;
    }

    // if randSpeed == 1, then random speeds assigned to each particle
    // if randDirect == 1, then random directions assigned to each particle

}